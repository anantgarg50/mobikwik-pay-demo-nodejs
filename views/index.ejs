<!DOCTYPE html>
<html>

<head>
  <title>MobiKwik Pay</title>

  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <div>
    <h3 class="align-center">You are about to be redirected to Payment Gateway!</h3>
    <p class="align-center">Press 'Proceed!' to complete the transaction </p>
    <p class="align-center">OR</p>
    <p class="align-center">Press 'Cancel' to cancel the transaction.</p>
  </div>

  <div class="amount-container">
    <p class="align-center amount">Transaction Amount is
      <strong><%= String.fromCharCode(8377) %><%= transactionAmount %></strong></p>
  </div>

  <div class="flex-container flex-justify-content-center">
    <button class="button-error flat-button white-button-text">Cancel</button>
    <form action="/transact" method="POST">
      <input type="hidden" name="amount" value="<%= amount %>" />
      <input type="hidden" name="buyerEmail" value="<%= buyerEmail %>" />
      <input type="hidden" name="currency" value="<%= currency %>" />
      <input type="hidden" name="merchantIdentifier" value="<%= merchantIdentifier %>" />
      <input type="hidden" name="orderId" value="<%= orderId %>" />
      <input type="hidden" name="returnUrl" value="<%= returnUrl %>" />
      <button type="submit" class="button-primary flat-button white-button-text">Proceed!</button>
    </form>
  </div>
</body>

</html>